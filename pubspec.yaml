name: mycoprognoza_pl
description: Aplikacja prognozująca wysyp grzybów w Polsce
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  geolocator: ^11.0.0
  permission_handler: ^11.0.1
  csv: ^6.0.0
  path_provider: ^2.1.2
  google_maps_flutter: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true